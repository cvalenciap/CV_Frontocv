export class MarcadorGeneral {
  lat: number;
  lng: number;
  label: string;
  draggable: boolean;
  nom_municipio: string;
  nom_incidencia: string;
  tipo_incidencia: string;
  fecha_incidencia: string;
  fecha_estimada_sol: string;
}
